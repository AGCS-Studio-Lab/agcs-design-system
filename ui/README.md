# Moved to agcs-ui

The product-UI layer moved to
[AGCS-Studio-Lab/agcs-ui](https://github.com/AGCS-Studio-Lab/agcs-ui) on
2026-09-24: the brand primitives apps vendor (`chevron`, `brand-footer`,
`lab-stamp`, `brand-icon`), the UX rules, and the color and grid tokens for
apps.

Apps that copied these files from here take them from `agcs-ui/ui/` from now
on. This folder stays only so older links still resolve.
